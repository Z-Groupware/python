# 빌드 컨텍스트를 최소로 — 이미지에 들어갈 이유가 없는 것들.
# ⚠️ .env 를 제외하는 게 가장 중요하다. 운영 비밀값은 SSM Parameter Store 가 주입한다.
.env
.venv/
__pycache__/
*.py[cod]

.git/
.github/
.gitignore
.dockerignore

tests/
.pytest_cache/
.ruff_cache/

README.md
*.md

# 로컬 오디오 샘플 (VAD 시험용). 정상 경로는 S3 경유다.
tmp/
*.wav
*.webm
*.m4a
